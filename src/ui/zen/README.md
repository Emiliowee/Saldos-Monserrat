# `src.ui.zen`

Cromo superior inspirado en navegadores tipo **Zen**: franja mínima y barra completa al hover.

- `chrome_host.py` — `AutoHideChrome`, `TopHoverStrip`, constante `CHROME_COLLAPSED_H`.

La barra con menús (`TitleBar`) sigue en `main_window.py` para no duplicar dependencias.
