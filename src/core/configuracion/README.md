# `src.core.configuracion`

Preferencias de **ventana** y comportamiento global (no mezcladas con alta de producto).

- `ventana.py` — barra superior siempre visible / modo Zen.

QSettings compartido: `src/core/qsettings_paths.py`.
