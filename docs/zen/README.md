# Referencia UI tipo Zen

- **Tema visual** (fondo shell, tarjeta blanca, radios, tipografía): `src/ui/tokens.py` + `src/ui/style.py` + panel en `main_window.py`.
- **Cromo** (franja + barra al hover): `src/ui/zen/chrome_host.py`.

Aquí puedes guardar **capturas** o notas de diseño. No versiones el instalador del navegador en el repo.
